process TEST = {
include "CondCore/DBCommon/data/CondDBCommon.cfi"
replace CondDBCommon.connect = "sqlite_file:testLogging.db"
    source = EmptyIOVSource {
        string timetype = "runnumber"
	uint64 firstValue = 1
	uint64 lastValue = 10
        uint64 interval = 2
    }
    service = PoolDBOutputService{
	using CondDBCommon
        untracked string timetype="runnumber"
	VPSet toPut={
	    {string record = "PedestalsRcd"
	     string tag ="mytest"}
	}
	untracked string logconnect = "sqlite_file:log.db"
    }
    module mytest = MyDataAnalyzer{ 
	string record="PedestalsRcd"
	untracked bool loggingOn = True
    }
    path p = { mytest }
}
